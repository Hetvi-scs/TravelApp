import {fontSize, fonts, lightTheme, size} from '../../theme';


export const radio1 = () => ({
    // flexDirection: 'row',
    // alignItems: 'center',
    width: 24,
    height: 24,
    borderRadius: 24,
    borderWidth: 2.5,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 10,
    // borderColor: radioButton ==='option1' ? '#20C997' : '#78909C',
  });